#include <stdio.h>

int main(void)
{
 int x, y;
 
 printf("정수 2개를 입력하시오:");
 scanf("%d %d", &x, &y);

 if (x == 0 || y == 0)
 {
  printf("특정 사분면에 속하지 않습니다\n");
 }
 else
 {
  (x > 0) ? ((y > 0) ? printf("좌표는 1사분면") : printf("좌표는 3사분면")) :

 ((y > 0) ? printf("좌표는 2사분면") : printf("좌표는 4사분면"));
 }
} 
